describe('Ocean.js Integration', () => {
  it.skip('should consume a fixed-rate asset using local signer', () => {
    // TODO: Import ocean-lib, instantiate Ocean with signer derived from this service.
    // Use the test dataset asset on Sepolia.
    // Follow: https://github.com/oceanprotocol/ocean.js/blob/main/CodeExamples.md#7-consume-a-fixed-rate-asset-data-asset
    // Requires gas – skip if insufficient balance.
  });

  it.skip('should consume a fixed-rate asset using vault signer', () => {
    // TODO: Same as above but with vault signer.
  });
});
