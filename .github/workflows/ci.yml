name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop
      - feat/**
      - feat/*

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Unit Tests
        run: npm test

      - name: E2E Tests (Local Mode)
        run: npm run test:e2e:local
        env:
          NODE_ENV: test
          PRIVATE_KEYS: ${{ secrets.PRIVATE_KEYS }}
          NODE_URI_MAP: ${{ secrets.NODE_URI_MAP }}
          JWT_TOKEN: ${{ secrets.JWT_TOKEN }}
          AUTHENTIK_JWKS_URI: ${{ vars.AUTHENTIK_JWKS_URI }}
          AUTHENTIK_ISSUER: ${{ vars.AUTHENTIK_ISSUER }}
          AUTHENTIK_AUDIENCE: ${{ vars.AUTHENTIK_AUDIENCE }}
          CHAIN_ID: ${{ vars.CHAIN_ID }}
          ETHEREUM_RPC_URL: ${{ vars.ETHEREUM_RPC_URL }}
          SIGNER_MODE: local
          PORT: 3001
          UPSTREAM_IDP: ${{ vars.UPSTREAM_IDP || 'VM3 Partner Source' }}

      - name: E2E Tests (Auth)
        run: npm run test:e2e:auth
        env:
          NODE_ENV: test
          PRIVATE_KEYS: ${{ secrets.PRIVATE_KEYS }}
          NODE_URI_MAP: ${{ secrets.NODE_URI_MAP }}
          JWT_TOKEN: ${{ secrets.JWT_TOKEN }}
          AUTHENTIK_JWKS_URI: ${{ vars.AUTHENTIK_JWKS_URI }}
          AUTHENTIK_ISSUER: ${{ vars.AUTHENTIK_ISSUER }}
          AUTHENTIK_AUDIENCE: ${{ vars.AUTHENTIK_AUDIENCE }}
          CHAIN_ID: ${{ vars.CHAIN_ID }}
          ETHEREUM_RPC_URL: ${{ vars.ETHEREUM_RPC_URL }}
          SIGNER_MODE: local
          PORT: 3001
          UPSTREAM_IDP: ${{ vars.UPSTREAM_IDP || 'VM3 Partner Source' }}

      - name: Coverage
        run: npm run test:cov

  # ============================================================
  # Vault Integration Tests
  # ============================================================
  test-vault:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'

    strategy:
      matrix:
        node-version: [22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build signer service
        run: npm run build

      - name: Create test private keys for Vault
        run: |
          mkdir -p docker-compose/vault/openbao/secrets
          cat > docker-compose/vault/openbao/secrets/private_keys << 'EOF'
          
          EOF
          echo "✅ Test private keys created"

      - name: Start OpenBAO
        working-directory: docker-compose/vault
        run: |
          echo "🚀 Starting OpenBAO container..."
          docker compose -f docker-compose.vault.yml up -d openbao

          echo "⏳ Waiting for OpenBAO initialization..."
          TIMEOUT=180
          while ! docker exec openbao test -f /vault/keys/root_token; do
            if [ $TIMEOUT -le 0 ]; then
              echo "❌ OpenBAO initialization timed out"
              docker compose -f docker-compose.vault.yml logs openbao
              exit 1
            fi
            echo "   Waiting... (${TIMEOUT}s remaining)"
            sleep 5
            TIMEOUT=$((TIMEOUT - 5))
          done

          echo "✅ OpenBAO initialized successfully"

          # Verify plugin registration
          if docker exec openbao bao plugin list -format=json 2>/dev/null | grep -q secpsign; then
            echo "✅ secpsign plugin registered"
          else
            echo "❌ secpsign plugin not registered"
            docker compose -f docker-compose.vault.yml logs openbao
            exit 1
          fi

          # Get root token
          ROOT_TOKEN=$(docker exec openbao cat /vault/keys/root_token)
          echo "VAULT_TOKEN=${ROOT_TOKEN}" >> $GITHUB_ENV

          # Verify accounts
          echo "📋 Imported accounts:"
          docker exec openbao /opt/openbao/scripts/manage-accounts.sh list || true

      - name: Start signer-server with Vault mode
        working-directory: docker-compose/vault
        run: |
          echo "🚀 Starting signer-server..."
          docker compose -f docker-compose.vault.yml up -d signer-server

          echo "⏳ Waiting for signer-server..."
          TIMEOUT=60
          while ! curl -s -k https://localhost:8443/health > /dev/null 2>&1; do
            if [ $TIMEOUT -le 0 ]; then
              echo "❌ signer-server startup timed out"
              docker compose -f docker-compose.vault.yml logs signer-server
              exit 1
            fi
            echo "   Waiting... (${TIMEOUT}s remaining)"
            sleep 2
            TIMEOUT=$((TIMEOUT - 2))
          done
          echo "✅ signer-server ready"

      - name: Run Vault E2E tests
        run: npm run test:e2e:vault
        env:
          NODE_ENV: test
          VAULT_URL: https://localhost:8200
          VAULT_TOKEN: ${{ env.VAULT_TOKEN }}
          SIGNER_MODE: vault
          VAULT_ETHEREUM_MOUNT: ethereum
          VAULT_KV_STORE_PATH: secret
          VAULT_TIMEOUT_MS: 10000
          NODE_EXTRA_CA_CERTS: docker-compose/vault/openbao/tls/tls.crt
          CHAIN_ID: 11155111
          ETHEREUM_RPC_URL: ${{ vars.ETHEREUM_RPC_URL || 'https://eth-sepolia.g.alchemy.com/v2/demo' }}
          PORT: 3001
          UPSTREAM_IDP: ${{ vars.UPSTREAM_IDP || 'VM3 Partner Source' }}
          AUTHENTIK_JWKS_URI: http://localhost:8080
          AUTHENTIK_ISSUER: http://localhost:8080
          AUTHENTIK_AUDIENCE: test-audience
          JWT_TOKEN: ${{ secrets.JWT_TOKEN || 'test-jwt-token' }}
          GITHUB_ACTIONS: true

      - name: Dump logs on failure
        if: failure()
        working-directory: docker-compose/vault
        run: |
          echo "========== OPENBAO LOGS =========="
          docker compose -f docker-compose.vault.yml logs openbao
          echo ""
          echo "========== SIGNER-SERVER LOGS =========="
          docker compose -f docker-compose.vault.yml logs signer-server

      - name: Cleanup
        if: always()
        working-directory: docker-compose/vault
        run: |
          docker compose -f docker-compose.vault.yml down -v
